
USE HOJCON IN 1  ALIAS P1
SELE 1
INDEX ON NUMMES+NUMHC TO Q1 

USE H:\TABLAS\HOJCON IN 2 ALIAS P2
SELE 2
INDEX ON NUMMES+NUMHC TO Q2

USE ITEHC IN 3 ALIAS H1
SELE 3
INDEX ON NUMMES+NUMHC TO Q3

USE H:\TABLAS\ITEHC IN 4 ALIAS H2
SELE 4
INDEX ON NUMMES+NUMHC TO Q4


SELECT H2
SCAN
     SCATTER MEMVAR
     wait window "VERIFICANDO : "+m.nummes+m.numhc NOWAIT
     SELE H1
     SEEK H2.nummes+H2.numhc
     IF !FOUND()
        wait window "no existe: "+m.nummes+m.numhc
     ENDIF
     SELECT H2
ENDSCAN
     


CLOS DATA
ERASE(Q1.IDX)
ERASE(Q2.IDX)
ERASE(Q3.IDX)
ERASE(Q4.IDX)
