USE DIARIO IN 1 ORDER TAG DIARIO1
SET FILTER TO COMPROB='14' AND (CUENTA='101' OR CUENTA='0') AND MONTH(FECEMI)=1
GO TOP
SCAN
	vcompro1 = comprob
	IF CUENTA='101'
		VMONTO = MONTOS
		SKIP
		IF vCOMPRO1 = comprob
			IF LEFT(CUENTA,1)='0'
				REPLACE MONTOS WITH VMONTO
				SKIP
				IF vCOMPRO1 = comprob
					IF LEFT(CUENTA,1)='0'
						REPLACE MONTOS WITH VMONTO
					ENDIF
				ENDIF
			ENDIF
		ELSE
		  SKIP -1
		ENDIF
	ENDIF
ENDSCAN
